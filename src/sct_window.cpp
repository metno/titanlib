#include <vector>
#include <math.h>
#include "titanlib.h"
#include <assert.h>
#include <iostream>

int titanlib::sct_window(const fvec lats,
            const fvec lons,
            const fvec elevs,
            const fvec values,
            int nmin,
            int nmax,
            int nminprof,
            float radius,
            float dzmin,
            float dhmin,
            float dz,
            const fvec pos,
            const fvec neg,
            const fvec eps2,
            fvec& sct,
            ivec& flags) {

    return 0;
}
