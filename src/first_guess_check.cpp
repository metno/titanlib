#include <vector>
#include <iostream>
#include <math.h>
#include "titanlib.h"


bool titanlib::first_guess_check(const fvec lats,
        const fvec lons,
        const fvec elevs,
        const fvec values,
        int unixtime,
        const fvec obs_min,
        const fvec obs_max) {

                return false;
        }